# Resource-Manager
A small full-stack web app where users can browse, upload, and manage resources (images, 3D models, audio, etc.). It checks if a resource link is online, shows its status, and provides backup links if the primary fails. Stack: React (frontend) · Node.js/Express (backend) · MongoDB (database) · TailwindCSS (UI)
